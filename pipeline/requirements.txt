# ============================================
# AWS & SageMaker Dependencies
# ============================================
boto3>=1.28.0              # AWS SDK for Python (S3, IAM, etc.)
sagemaker>=2.180.0         # SageMaker SDK for ML deployment
botocore>=1.31.0           # Low-level AWS SDK (installed with boto3)

# ============================================
# Image & Video Processing
# ============================================
Pillow>=10.0.0             # Image processing (PIL fork)
opencv-python>=4.8.0.76    # Computer vision library (video I/O, drawing)

# Alternative for headless servers (choose one):
# opencv-python-headless>=4.8.0.76  # Use this for servers without GUI

# ============================================
# NumPy (required by OpenCV)
# ============================================
numpy>=1.24.0              # Numerical computing (auto-installed with opencv)

# ============================================
# Python Standard Library
# ============================================
# The following are built-in to Python, no installation needed:
# - os
# - sys
# - json
# - base64
# - time
# - io (BytesIO)